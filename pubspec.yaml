name: opc_mobile_development
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  stacked: ^3.4.0
  stacked_services: ^1.1.0
  cached_network_image: ^3.3.1
  dio: ^5.4.3+1
  shared_preferences: ^2.2.3

  stacked_shared: any
dev_dependencies:
  build_runner: ^2.4.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.4.1
  stacked_generator: ^1.3.3


flutter:
  uses-material-design: true

  assets:
    - lib/resources/images/